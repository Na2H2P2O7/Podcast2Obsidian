# Podcast2Obsidian — specific_podcasts watcher sources / 指定节目订阅源
#
# Used by skill/scripts/specific_podcasts_daily.py. One Xiaoyuzhou podcast URL
# per line; everything after "#" is a comment. Copy this file to
# `specific_podcasts.txt` (same folder) and edit it, or point the watcher at any
# file via `--sources-file PATH` / the `SPECIFIC_PODCASTS_FILE` env var.
#
# 由 skill/scripts/specific_podcasts_daily.py 使用。每行一个小宇宙播客链接，
# "#" 之后为注释。复制为同目录下的 `specific_podcasts.txt` 后修改，或用
# `--sources-file PATH` / 环境变量 `SPECIFIC_PODCASTS_FILE` 指向任意文件。

https://www.xiaoyuzhoufm.com/podcast/<your_podcast_id>   # 示例节目 (example / 示例)
