PROFILE_ID="xiaoyuzhou"
# canonical profile for 小宇宙; bound sub-agent chat: -100XXXXXXXXXX
# Leave URL empty by default; pass --url for real runs.
URL=""
NLM_PROFILE="default"  # default NotebookLM profile for this wrapper path: primary-nlm-account; do not change nlm CLI default account
LOCAL_ONLY="0"            # 1=--local
NO_NOTEBOOKLM="0"         # 1=--no-notebooklm
FORCE_ALL="0"             # 1=--yes (podcast URL)
LOG_CHANNEL_CHAT_ID=""
# Deterministic completion callback for run_profile.sh; wrapper sends final RESULT here.
RESULT_NOTIFY_CHAT_ID=""
